Five items are sent per request. The user message is literally:

Classify these items:
{"idx": 0, "title": "<headline>", "description": "<first 400 characters>"}
{"idx": 1, "title": "<headline>", "description": "<first 400 characters>"}
... five lines total ...

Titles and descriptions are JSON-escaped, so a quote or newline inside an
article cannot break the structure.

Settings: temperature 0, max_tokens 1200, reasoning disabled where the server
allows it.
