The latency test builds three prompts of growing size from one repeated block
of real Python, then asks for a rewrite. Repeats: 2 (~300 tokens),
60 (~8 000 tokens), 220 (~32 000 tokens).

The repeated block:

```python
def process_record(record, config, cache):
    key = record.get('id')
    if key is None:
        raise ValueError('record without id')
    if key in cache:
        return cache[key]
    result = {'id': key, 'fields': {}}
    for name, spec in config['fields'].items():
        raw = record.get(name)
        if raw is None and spec.get('required'):
            raise KeyError(name)
        result['fields'][name] = spec['parse'](raw) if raw is not None else None
    cache[key] = result
    return result

```

The full message:

Here is part of a Python codebase:

```python
# --- module 0 ---
<block>
# --- module 1 ---
<block>
...
```

Rewrite process_record so that parse failures are collected into a list of
errors instead of raising on the first one. Return only the new function.

Settings: temperature 0, max_tokens 256, streaming on, one untimed warm-up first.
